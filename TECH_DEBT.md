# 技术债记录

> 记录当前项目里已知但暂不修复的问题：明确知道有缺陷/风险，出于优先级、成本或需要更多信息等原因先搁置。与 [PROJECT.md](PROJECT.md) §7 决策记录的区别：决策记录是"做出了什么决定、为什么"，本文档是"留了什么坑、什么条件下该填"。
>
> 每条记录需要包含：现象、根因、触发条件、影响范围、可能的修复方向、状态。问题被修复后从本文档移除，修复记录改为写入 PROJECT.md §7 决策记录。

---

## 1. 配对端点没有速率限制，6 位配对码存在被在线爆破的风险

- **现象**：`POST /ai-agent/v1/device/pair` 可以被无限次调用，每次尝试一个配对码。
- **根因**：配对码只有 6 位数字（100 万种组合），当前唯一的防护是 5 分钟有效期与一次性消费。缺少针对该端点的失败次数限制。
- **触发条件**：攻击者在某个用户生成配对码后的 5 分钟窗口内，对同一租户高速枚举 6 位码。按每秒 1000 次估算，5 分钟可覆盖 30 万种组合（约 30% 的空间）——这不是理论风险。
- **影响范围**：一旦命中，攻击者会拿到一个绑定该用户的 `device_token`，等于获得驱动其浏览器的能力（受插件侧白名单与审批闸门约束，但仍是严重越权）。
- **可能的修复方向**：按 `tenant_code` + 来源 IP 做 Redis 计数器限流（如 5 分钟内失败 10 次即锁定），与 PROJECT.md §4.4 已规划的租户维度限流一并实现；或把配对码加长到 8 位并改用无歧义字符集。
- **状态**：未修复。落地到公网前**必须**先补上——`device.GenerateCode` 的注释里已标注该前提。

## 2. `BROWSER_ALLOWED_ORIGINS` 未配置时不校验 WebSocket 的 Origin

- **现象**：环境变量为空时 `websocket.Accept` 会设置 `InsecureSkipVerify: true`，接受任意 Origin 发起的连接。
- **根因**：扩展的 Origin 形如 `chrome-extension://<id>`，其 id 因安装方式而异，无法在代码里硬编码一个合理默认值；为了不阻断本地开发，选择了"留空即放行"。
- **触发条件**：生产部署时忘记配置该环境变量。
- **影响范围**：任意网页都能从用户浏览器向本服务发起 WS 连接（CSWSH）。实际危害受限——攻击者仍需要一个合法的 `device_token` 才能通过认证首帧——但这道防线不该缺失。
- **可能的修复方向**：改为生产环境（如通过 `APP_ENV` 判断）未配置时**启动即失败**，而不是打一条 WARN 继续运行。
- **状态**：未修复。当前仅在启动日志里记 WARN 提示。

## 3. 浏览器指令未接入 CheckPoint / 服务端 HITL 审批网关

- **现象**：`click`/`type`/`select`/`open_tab` 这些写操作的审批完全由插件本地完成，服务端不参与，也不留审批记录。
- **根因**：服务端的审批网关（HITL）与 CheckPoint 排在 PROJECT.md §5 阶段三，而插件的写操作能力在其阶段二就已具备——插件侧因此自建了本地审批闸门（插件 DESIGN.md §1.2 明确记录了这个提前量）。
- **触发条件**：任何一次浏览器写操作。
- **影响范围**：用户的审批动作不产生服务端侧的审计记录，与其他高危工具的审批体验也不一致；`PROJECT.md §6.2` 要求高危操作走审批网关，当前是靠插件本地闸门等效满足，而非走统一机制。
- **可能的修复方向**：阶段三服务端 HITL 就绪后，把插件的 `ack{pending_approval}` 与服务端审批网关打通，审批记录落库；插件侧 `guard/approval.ts` 的接口已按可替换设计。
- **状态**：未修复，属于已知的阶段性状态而非疏漏。

---

（历史条目：「旧会话超过 Redis TTL 后继续对话，模型看不到之前的历史」已于 2026-07-27 修复，见 [PROJECT.md](PROJECT.md) §7 决策记录对应条目。）

---
