# Template only — do NOT commit real values into this file or any fork of
# it. Fill in and apply via `kubectl apply -f` locally, or better, manage
# this Secret out-of-band (sealed-secrets, external-secrets + a cloud
# secret manager, `kubectl create secret` in a deploy pipeline, etc.) and
# drop this file from the applied set.
apiVersion: v1
kind: Secret
metadata:
  name: server-secrets
type: Opaque
stringData:
  MODEL_API_KEY: ""
