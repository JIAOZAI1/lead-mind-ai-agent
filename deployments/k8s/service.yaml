apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: lead-mind-ai-agent
  labels:
    app: lead-mind-ai-agent
    component: server
spec:
  type: ClusterIP
  selector:
    app: lead-mind-ai-agent
    component: server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
