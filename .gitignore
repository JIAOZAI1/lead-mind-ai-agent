/bin/
/dist/
*.exe
*.test
*.out
.env
.env.*
!.env.example
/.claude
# Local k8s manifests may carry real secrets/endpoints once filled in
# (see deployments/k8s/secret.yaml, configmap.yaml) — keep out of git.
/deployments/
